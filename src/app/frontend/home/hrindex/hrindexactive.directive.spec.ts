import { HrindexactiveDirective } from './hrindexactive.directive';

describe('HrindexactiveDirective', () => {
  it('should create an instance', () => {
    const directive = new HrindexactiveDirective();
    expect(directive).toBeTruthy();
  });
});
